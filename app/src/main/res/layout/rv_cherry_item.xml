<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/material_margin_small"
    android:layout_marginTop="@dimen/material_margin_small"
    android:elevation="@dimen/cardview_default_elevation"
    app:cardCornerRadius="@dimen/cardview_default_radius">

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ivBackground"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="#000"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="@+id/tvCherryName"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/ivCherryName"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:alpha="0.7"
            android:background="@color/transparent_black"
            app:layout_constraintBottom_toBottomOf="@+id/tvCherryName"
            app:layout_constraintLeft_toLeftOf="@+id/tvCherryName"
            app:layout_constraintRight_toRightOf="@+id/tvCherryName"
            app:layout_constraintTop_toTopOf="@+id/tvCherryName" />

        <TextView
            android:id="@+id/tvCherryName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/material_card_size"
            android:gravity="left"
            android:padding="@dimen/material_margin_sm"
            android:text="Cherry Name"
            android:textColor="#fff"
            android:textSize="@dimen/ts_mml"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </android.support.constraint.ConstraintLayout>
</android.support.v7.widget.CardView>